# config

Project configuration files.

- Environment-specific values: `config/environments/`
- Expo dynamic config placeholder: `config/app.config.md` (replace with `app.config.js` when ready)
- react-native-config placeholder: `config/react-native-config.md`
