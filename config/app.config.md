# config/app.config.md

Placeholder for Expo dynamic config.

Replace this file with a real `config/app.config.js` when you are ready.

Typical responsibilities:
- Read environment variables (dev/staging/prod)
- Produce Expo config (name, slug, version, icons, extra)
