# assets

Static assets used by the app.

- Put custom fonts in `assets/fonts/`
- Put images (splash, illustrations, app icons) in `assets/images/`
- Put UI icons (tab bar / buttons) in `assets/icons/`
