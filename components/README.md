# components (legacy/root)

Current shared components live here.

Recommended target structure (from your blueprint):
- Move reusable cross-feature components into `app/common/components/`
- Keep feature-specific components inside `app/features/<feature>/components/`
