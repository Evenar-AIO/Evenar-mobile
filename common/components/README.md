# app/common/components

Reusable UI components shared across the app.

Recommended pattern:
- One folder per component
- Export via a barrel (e.g. `index.ts`)

This repo uses `*.md` placeholders until you implement real components.
