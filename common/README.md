# app/common

Shared, cross-cutting code used across features.

Put globally reusable code here:
- UI components: `components/`
- Global hooks: `hooks/`
- Styling system: `styles/`
- Utilities/helpers: `utils/`
- App-wide constants: `constants/`
