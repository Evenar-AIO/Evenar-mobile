# app

Expo Router source root.

- Route groups live here, e.g. `(auth)/`, `(tabs)/`
- Cross-cutting shared code: `common/`
- Feature modules: `features/`
- Global API layer: `services/`
- State management: `store/`
- Test files: `__tests__/`
