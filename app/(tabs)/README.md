# app/(tabs)

Expo Router tab navigator routes.

Recommended tab routes in this group:
- `home.tsx`
- `profile.tsx`
- `settings.tsx`

Note: this repo already contains real routes (e.g. `index.tsx`, `explore.tsx`) and a real `_layout.tsx`.
The `*.md` files here are placeholders you can replace with real routes later.
